{
    "$schema": "http://json-schema.org/schema",
    "$id": "add-storybook",
    "title": "npx sb init",
    "description": "Initialize Storybooks",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project",
            "x-prompt": "Project name:",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        } 
    }
}
