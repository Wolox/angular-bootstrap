{
  "$schema": "http://json-schema.org/schema",
  "$id": "add-linter",
  "title": "ng add linter schematic",
  "type": "object",
  "description": "Initialize linter",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project",
      "x-prompt": "Name:",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    } 
  }
}
